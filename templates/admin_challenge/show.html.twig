{% extends 'base_admin.html.twig' %}

{% block title %}Challenge{% endblock %}

{% block body %}
	<h1>Challenge</h1>

	<table class="table">
		<tbody>
		<tr>
			<th>Id</th>
			<td>{{ challenge.id }}</td>
		</tr>
		<tr>
			<th>Name</th>
			<td>{{ challenge.name }}</td>
		</tr>
		<tr>
			<th>Description</th>
			<td>{{ challenge.description }}</td>
		</tr>
		<tr>
			<th>Number_of_players</th>
			<td>{{ challenge.numberOfPlayers }}</td>
		</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_challenge_index') }}">back to list</a>

	<a href="{{ path('app_admin_challenge_edit', {'id': challenge.id}) }}">edit</a>

	{{ include('admin_challenge/_delete_form.html.twig') }}
{% endblock %}
